# ============================================================
# config.py — ตั้งค่าระบบ (แก้ไขตอนติดตั้งบนเครื่องเซิร์ฟเวอร์)
# ============================================================

# โฟลเดอร์ปลายทางสำหรับบันทึกไฟล์ใบเสร็จ Excel
#   - แนะนำใช้ UNC path เต็ม เช่น   r"\\server\share\Receipts"
#   - หรือ local path เช่น          r"C:\Receipts"
#   * อย่าลืมเครื่องหมาย r หน้าสตริง (raw string) เพื่อให้ \ ทำงานถูกต้อง
RECEIPT_SAVE_DIR = r"\\server\share\Receipts"

# แยกโฟลเดอร์ย่อยตามปี/เดือนอัตโนมัติหรือไม่
#   True  -> \\server\share\Receipts\2569\06\CA690615-01_ชื่อ.xlsx
#   False -> \\server\share\Receipts\CA690615-01_ชื่อ.xlsx
ORGANIZE_BY_DATE = True

# ============================================================
# โฟลเดอร์ "ขาเข้า" (watch folder) ที่โปรแกรม WinClinic export ไฟล์ดิบมาไว้
#   โปรแกรมใบเสร็จจะอ่านไฟล์จากโฟลเดอร์นี้อัตโนมัติ ไม่ต้องอัปโหลดเอง
#   - ตั้งเป็น "" (ค่าว่าง) ถ้าไม่ใช้ระบบโฟลเดอร์อัตโนมัติ
#   - ใส่ r หน้าสตริงเสมอ เช่น   r"\\192.168.0.50\winclinic\inbox"  หรือ  r"C:\winclinic_export"
RAW_INBOX_DIR = r""

# เมื่อออกใบเสร็จเสร็จแล้ว ให้ย้ายไฟล์ดิบไปเก็บในโฟลเดอร์ย่อย done หรือไม่
#   True  -> ไฟล์ที่ทำเสร็จจะถูกย้ายออกจากคิว (กันทำซ้ำ)
MOVE_DONE_FILES = True

# ชื่อโฟลเดอร์ย่อยสำหรับเก็บไฟล์ดิบที่ทำเสร็จแล้ว (อยู่ภายใน RAW_INBOX_DIR)
#   แก้ชื่อตรงนี้จุดเดียว เช่น "_done", "เสร็จแล้ว", "archive"
DONE_FOLDER_NAME = "_done"

# ลบไฟล์ในโฟลเดอร์ done ที่เก่ากว่ากี่วันโดยอัตโนมัติ
#   เช่น 7 = เก็บไฟล์ดิบไว้ 7 วันแล้วลบทิ้ง | 0 = ไม่ลบอัตโนมัติ (เก็บถาวร)
AUTO_DELETE_DAYS = 7


